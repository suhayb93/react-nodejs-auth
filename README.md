# react-nodejs-auth

Starting Client side 

`cd client`

`npm install`

`npm start`

use `localhost:3000`


Starting server side 

`cd server`

`npm install`

`npm run dev`

server will listen on port `3090`

